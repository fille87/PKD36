Strings are concatenated with +. Repeated concatenation with *
** operator is exponentiation
and/or keywords
Loops: while, loop, break, continue
loop; name {
    break name;
}

while x == 2; name {
    break;
}

Every statement ends with a semicolon
Assignments are made with the var keyword.
Function declarations look like: fn name(arg1, arg2) { }
Last expression is implicitly returned, if there is none then null is returned
Ternary expressions are supported?
Parentheses not needed after if/else keyword
Labeled loops and break
Comments start with a ::

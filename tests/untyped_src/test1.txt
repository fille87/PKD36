var a = 4;
print a;
fn add(a, b, c) {
    return a + b + c;
}
fn add(b, c) {
    return a + b + c;
}
print add(1, 2 ,3);
print add(1, 2);
print a;
print if a == 4 {
                print "inside of if";
                var ifA = 0;
                return loop {
                    ifA = ifA + 1;
                    print "inside of if loop: "; 
                    print ifA; 
                    if ifA == 5 {
                        break return "loop reached iteration " + ifA;
                    }
                };
            } else {"a is not four"};


print while a > 1 : outer {
    var i = 0;
    print "a is: " + a;
    loop: inner {
        i = i+1;
        print "i is: " + i;
        if a < i {
            print "a("+a+") is smaller than i(" + i +")";
            break : outer return "broke outer";
        };
    };
    a = a - 1;
};